export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are the Types of Data Binding in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("1. Interpolation");
    addLog("2. Property Binding");
    addLog("3. Event Binding");
    addLog("4. Two-Way Data Binding");

    addLog("");

    addLog("Program completed.");

}