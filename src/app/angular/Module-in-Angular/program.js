export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Module in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("A Module is a container that groups related components, directives, pipes, and services.");

    addLog("");

    addLog("Example Command:");
    addLog("ng g m admin");

    addLog("");

    addLog("Program completed.");

}