export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Angular Guard?");
    addLog("");

    addLog("Answer:");
    addLog("Angular Guard is used to control access to routes based on conditions such as authentication or authorization.");

    addLog("");

    addLog("Example:");
    addLog("CanActivate");

    addLog("");

    addLog("Program completed.");

}