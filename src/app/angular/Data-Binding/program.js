export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Data Binding in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Data Binding is the process of connecting a Component (TypeScript) with a Template (HTML).");

    addLog("");

    addLog("Example:");
    addLog("{{ name }}");

    addLog("");

    addLog("Program completed.");

}