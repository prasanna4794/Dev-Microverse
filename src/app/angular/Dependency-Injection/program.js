export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Dependency Injection (DI) in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Dependency Injection is a mechanism where Angular automatically creates and provides required service objects to components.");

    addLog("");

    addLog("Example:");
    addLog("constructor(private productService: ProductService) {}");

    addLog("");

    addLog("Program completed.");

}