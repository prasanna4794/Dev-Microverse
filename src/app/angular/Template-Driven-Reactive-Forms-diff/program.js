export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is the difference between Template-Driven Forms and Reactive Forms?");
    addLog("");

    addLog("Answer:");
    addLog("Template-Driven Forms are HTML-based.");
    addLog("Reactive Forms are TypeScript-based.");

    addLog("");

    addLog("Program completed.");

}