export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Routing in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Routing is used to navigate between different components or pages without reloading the entire application.");

    addLog("");

    addLog("Example:");
    addLog("localhost:4200/products");

    addLog("");

    addLog("Program completed.");

}