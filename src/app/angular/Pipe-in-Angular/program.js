export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Pipe in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("A Pipe is used to transform and format data before displaying it in the UI.");

    addLog("");

    addLog("Example:");
    addLog("{{ name | uppercase }}");

    addLog("");

    addLog("Program completed.");

}