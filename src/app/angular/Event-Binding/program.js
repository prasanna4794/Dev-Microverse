export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Event Binding in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Event Binding is a one-way data binding technique used to connect HTML events with Component methods.");

    addLog("");

    addLog("Example:");
    addLog('<button (click)="showMessage()">');

    addLog("");

    addLog("Program completed.");

}