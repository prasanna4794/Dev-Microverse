export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are the Types of Directives in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("1. Component Directive");
    addLog("2. Structural Directive");
    addLog("3. Attribute Directive");

    addLog("");

    addLog("Examples:");
    addLog("*ngIf");
    addLog("*ngFor");
    addLog("[ngStyle]");

    addLog("");

    addLog("Program completed.");

}