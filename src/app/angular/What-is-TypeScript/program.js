export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is TypeScript?");
    addLog("");

    addLog("Answer:");
    addLog("TypeScript is a superset of JavaScript developed by Microsoft.");

    addLog("");

    addLog("Why Angular uses TypeScript?");
    addLog("It provides type safety, better code quality, and easier maintenance.");

    addLog("");

    addLog("Program completed.");

}