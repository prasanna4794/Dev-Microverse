export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Component in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("A Component is the basic building block of an Angular application. It controls a part of the user interface.");

    addLog("");

    addLog("Example Command:");
    addLog("ng g c home");

    addLog("");

    addLog("Program completed.");

}