export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Angular CLI?");
    addLog("");

    addLog("Answer:");
    addLog("Angular CLI is the official Command Line Interface used to create, run, build, and manage Angular applications.");

    addLog("");

    addLog("Example Commands:");
    addLog("ng new my-app");
    addLog("ng serve");
    addLog("ng g c home");

    addLog("");

    addLog("Program completed.");

}