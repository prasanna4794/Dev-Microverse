export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Template in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("A Template is the HTML view of an Angular component used to display the user interface.");

    addLog("");

    addLog("Example:");
    addLog("<h1>Welcome to Angular</h1>");

    addLog("");

    addLog("Program completed.");

}