export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Directives in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Directives are Angular features used to change the behavior or appearance of HTML elements.");

    addLog("");

    addLog("Example:");
    addLog("*ngIf");

    addLog("");

    addLog("Program completed.");

}