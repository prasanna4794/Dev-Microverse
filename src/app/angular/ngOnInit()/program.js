export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is ngOnInit() in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("ngOnInit() is a Lifecycle Hook that Angular calls once after the component is initialized.");

    addLog("");

    addLog("Uses:");
    addLog("Load API Data");
    addLog("Initialize Variables");

    addLog("");

    addLog("Program completed.");

}