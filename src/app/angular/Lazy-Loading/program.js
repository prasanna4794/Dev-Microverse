export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Lazy Loading in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Lazy Loading loads a module only when it is needed instead of loading all modules at application startup.");

    addLog("");

    addLog("Benefit:");
    addLog("Improves application performance.");

    addLog("");

    addLog("Program completed.");

}