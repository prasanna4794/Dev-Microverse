export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Lifecycle Hooks in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Lifecycle Hooks are special methods that Angular automatically calls during different stages of a component's life cycle.");

    addLog("");

    addLog("Example:");
    addLog("ngOnInit()");
    addLog("ngOnDestroy()");

    addLog("");

    addLog("Program completed.");

}