export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Angular is a Front-End Framework developed by Google.");

    addLog("");

    addLog("Uses:");
    addLog("Build Single Page Applications (SPA).");

    addLog("");

    addLog("Program completed.");

}