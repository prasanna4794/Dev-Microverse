export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Service in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("A Service is a class used to share business logic, reusable code, API calls, and data between multiple components.");

    addLog("");

    addLog("Example Command:");
    addLog("ng g s product");

    addLog("");

    addLog("Program completed.");

}