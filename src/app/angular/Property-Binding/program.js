export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Angular Interpolation?");
    addLog("");

    addLog("Answer:");
    addLog("Interpolation is a one-way data binding technique used to display TypeScript data in an HTML template.");

    addLog("");

    addLog("Example:");
    addLog("{{ title }}");

    addLog("");

    addLog("Program completed.");

}