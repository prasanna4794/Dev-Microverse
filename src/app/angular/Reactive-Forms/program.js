export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Reactive Forms in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Reactive Forms are forms that are managed and controlled through TypeScript code.");

    addLog("");

    addLog("Example:");
    addLog("FormGroup");
    addLog("FormControl");

    addLog("");

    addLog("Program completed.");

}