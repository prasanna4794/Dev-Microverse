export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is the difference between Angular and React?");
    addLog("");

    addLog("Answer:");
    addLog("Angular is a complete frontend framework.");
    addLog("React is a JavaScript library for building user interfaces.");

    addLog("");

    addLog("Program completed.");

}