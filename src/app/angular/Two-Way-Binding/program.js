export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Two-Way Data Binding in Angular?");
    addLog("");

    addLog("Answer:");
    addLog("Two-Way Data Binding automatically synchronizes data between the Component and the Template.");

    addLog("");

    addLog("Example:");
    addLog('<input [(ngModel)]="username">');

    addLog("");

    addLog("Program completed.");

}