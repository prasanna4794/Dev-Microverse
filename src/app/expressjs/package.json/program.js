export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is package.json in Node.js?");
    addLog("");

    addLog("Answer:");
    addLog(
        "package.json stores project information, dependencies, and scripts."
    );

    addLog("");

    addLog("Example:");
    addLog('"scripts": { "start": "node server.js" }');

    addLog("");

    addLog("Command:");
    addLog("npm start");

    addLog("");

    addLog("Output:");
    addLog("Node.js server started successfully.");

    addLog("");

    addLog("Program completed.");
}