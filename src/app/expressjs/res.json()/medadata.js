const metadata = {
    id: 16,
    level: 1,
    primitiveName:
        "[Express.js Response:] Understanding res.json().",
    directParentComponent: "Express.js",
    ownersLevel: "Node.js",
    primaryLearn: [
        "res.json()",
        "JSON Response",
        "REST API",
        "Response"
    ],
    functionalGoal:
        "res.json() is used to send a JSON response from the server to the client."
};

export default metadata;