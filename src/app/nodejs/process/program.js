export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is process in Node.js?");
    addLog("");

    addLog("Answer:");
    addLog(
        "process is a built-in Node.js object that provides information about and control over the current Node.js process."
    );

    addLog("");

    addLog("Node.js Version:");

    addLog("process.version");

    addLog("");

    addLog("Platform:");

    addLog("process.platform");

    addLog("");

    addLog("Process ID:");

    addLog("process.pid");

    addLog("");

    addLog("Environment Variables:");

    addLog("process.env");

    addLog("");

    addLog("Command-line Arguments:");

    addLog("process.argv");

    addLog("");

    addLog("Important:");
    addLog("process → Access and control the current Node.js process.");

    addLog("");

    addLog("Program completed.");
}