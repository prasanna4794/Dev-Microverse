export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is npm in Node.js?");
    addLog("");

    addLog("Answer:");
    addLog(
        "npm stands for Node Package Manager. It is used to install and manage packages for Node.js applications."
    );

    addLog("");

    addLog("Example:");

    addLog("npm install express");

    addLog("");

    addLog("This command installs the Express.js package.");

    addLog("");

    addLog("Important npm Commands:");

    addLog("npm init");
    addLog("npm init -y");
    addLog("npm install express");
    addLog("npm install nodemon --save-dev");

    addLog("");

    addLog("Package:");
    addLog("Express.js");

    addLog("");

    addLog("Installed Location:");
    addLog("node_modules");

    addLog("");

    addLog("Program completed.");
}