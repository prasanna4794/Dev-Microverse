export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is the meta tag?");
    addLog("");

    addLog("Answer:");
    addLog("The meta tag provides information about the webpage.");
    addLog("It helps browsers and search engines understand the page.");
    addLog("");

    addLog("Common Meta Tags:");

    addLog(`<meta charset="UTF-8">

<meta
    name="viewport"
    content="width=device-width, initial-scale=1.0">`);

    addLog("");

    addLog("Explanation:");
    addLog("charset=\"UTF-8\" supports different languages and special characters.");
    addLog("viewport makes the webpage responsive on mobile devices.");
    addLog("");

    addLog("Program completed.");

}