export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Hyperlink?");
    addLog("");

    addLog("Answer:");
    addLog("A hyperlink is used to navigate from one webpage to another webpage, website, file, or section.");
    addLog("It is created using the <a> (anchor) tag.");
    addLog("");

    addLog("Example:");

    addLog(`<a href="https://google.com">
    Google
</a>`);

    addLog("");

    addLog("Explanation:");
    addLog("<a> is the anchor tag.");
    addLog("The href attribute specifies the destination URL.");
    addLog("Clicking the link opens the specified webpage.");
    addLog("");

    addLog("Program completed.");

}