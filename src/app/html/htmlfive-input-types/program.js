export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are HTML5 input types?");
    addLog("");

    addLog("Answer:");
    addLog("HTML5 provides different input types for different kinds of user input.");
    addLog("");

    addLog("Common HTML5 Input Types:");
    addLog("- email");
    addLog("- date");
    addLog("- number");
    addLog("- password");
    addLog("- tel");
    addLog("- url");
    addLog("- search");
    addLog("");

    addLog("Example:");

    addLog(`<input type="email">

<input type="date">

<input type="number">`);

    addLog("");

    addLog("Explanation:");
    addLog("type=\"email\" accepts email addresses.");
    addLog("type=\"date\" displays a date picker.");
    addLog("type=\"number\" accepts only numeric values.");
    addLog("");

    addLog("Program completed.");

}