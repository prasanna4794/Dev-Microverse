export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is the title tag?");
    addLog("");

    addLog("Answer:");
    addLog("The title tag defines the title of a webpage.");
    addLog("It is displayed in the browser tab.");
    addLog("");

    addLog("Example:");

    addLog(`<head>

    <title>My Website</title>

</head>`);

    addLog("");

    addLog("Explanation:");
    addLog("<title> defines the webpage title.");
    addLog("The title appears in the browser tab.");
    addLog("It is also used in bookmarks and search engine results.");
    addLog("");

    addLog("Program completed.");

}