export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are the advantages of jQuery?");
    addLog("");

    addLog("Answer:");

    addLog("1. Less code.");

    addLog("2. Easy to learn.");

    addLog("3. Cross-browser support.");

    addLog("4. Easy DOM manipulation.");

    addLog("5. Easy event handling.");

    addLog("6. AJAX support.");

    addLog("7. Built-in animation effects.");

    addLog("");

    addLog("Program completed.");

}