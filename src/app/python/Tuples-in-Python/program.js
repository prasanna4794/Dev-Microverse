export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Tuples in Python?");
    addLog("");

    addLog("Answer:");
    addLog("A Tuple is an ordered, immutable collection used to store multiple values.");

    addLog("");

    const colors = ["Red", "Green", "Blue"];

    addLog("Example:");
    addLog(colors.join(", "));

    addLog("");

    addLog("Program completed.");

}