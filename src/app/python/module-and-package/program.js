export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Modules and Packages in Python?");
    addLog("");

    addLog("Answer:");
    addLog("A Module is a single Python file containing reusable code. A Package is a collection of related modules.");

    addLog("");

    addLog("Example:");
    addLog("import math");
    addLog("math.sqrt(25) = 5");

    addLog("");

    addLog("Program completed.");

}