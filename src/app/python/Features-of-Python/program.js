export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are the Features of Python?");
    addLog("");

    addLog("Answer:");
    addLog("Python is easy to learn, easy to read, interpreted, object-oriented, cross-platform, open source, dynamically typed, and has a huge library ecosystem.");

    addLog("");

    addLog("Example:");
    addLog("print(10 + 20)");

    addLog("");

    addLog("Program completed.");

}