export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Virtual Environments in Python?");
    addLog("");

    addLog("Answer:");
    addLog("A Virtual Environment is an isolated environment used to manage project-specific Python packages.");

    addLog("");

    addLog("Example:");
    addLog("python -m venv myenv");
    addLog("myenv\\Scripts\\activate");
    addLog("pip install requests");

    addLog("");

    addLog("Program completed.");

}