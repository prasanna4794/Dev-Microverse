export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Loops in Python?");
    addLog("");

    addLog("Answer:");
    addLog("Loops are used to execute the same block of code multiple times.");

    addLog("");

    addLog("Example (for loop):");

    for (let i = 1; i <= 5; i++) {
        addLog("Welcome");
    }

    addLog("");

    addLog("Program completed.");

}