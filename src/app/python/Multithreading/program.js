export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Multithreading in Python?");
    addLog("");

    addLog("Answer:");
    addLog("Multithreading allows multiple tasks to run concurrently within the same program.");

    addLog("");

    addLog("Example:");
    addLog("Thread 1 Started");
    addLog("Thread 2 Started");
    addLog("Both threads executed.");

    addLog("");

    addLog("Program completed.");

}