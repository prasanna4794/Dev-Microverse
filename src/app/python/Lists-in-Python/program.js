export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Lists in Python?");
    addLog("");

    addLog("Answer:");
    addLog("A List is an ordered, mutable collection that can store multiple values.");

    addLog("");

    const fruits = ["Apple", "Orange", "Banana"];

    addLog("Example:");
    addLog(fruits.join(", "));

    addLog("");

    addLog("Program completed.");

}