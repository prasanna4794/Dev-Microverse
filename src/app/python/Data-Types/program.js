export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Data Types in Python?");
    addLog("");

    addLog("Answer:");
    addLog("Data Types define the type of data stored in a variable.");

    addLog("");

    addLog("Examples:");
    addLog('String : name = "Prasanna"');
    addLog("Integer : age = 25");
    addLog("Float : height = 5.8");
    addLog("Boolean : isStudent = True");

    addLog("");

    addLog("Program completed.");

}