export default function runProgram(addLog) {

    addLog("Question:");
    addLog("Why do we use Python?");
    addLog("");

    addLog("Answer:");
    addLog("Python is used because it has simple syntax, requires less code, and is suitable for web development, automation, AI, machine learning, data science, and testing.");

    addLog("");

    addLog("Example:");
    addLog('print("Welcome Prasanna")');

    addLog("");

    addLog("Program completed.");

}