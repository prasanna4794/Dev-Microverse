export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Variables in Python?");
    addLog("");

    addLog("Answer:");
    addLog("Variables are used to store data in memory. A variable acts like a container that holds values.");

    addLog("");

    addLog("Example:");
    addLog('name = "Prasanna"');
    addLog("age = 25");

    addLog("");

    addLog("Program completed.");

}