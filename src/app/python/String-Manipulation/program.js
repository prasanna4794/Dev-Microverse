export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is String Manipulation in Python?");
    addLog("");

    addLog("Answer:");
    addLog("String Manipulation means performing operations such as converting, replacing, splitting, and joining text.");

    addLog("");

    const text = "Prasanna";

    addLog("Uppercase : " + text.toUpperCase());
    addLog("Lowercase : " + text.toLowerCase());
    addLog("Length : " + text.length);

    addLog("");

    addLog("Program completed.");

}