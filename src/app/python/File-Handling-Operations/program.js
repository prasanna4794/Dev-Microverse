export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is File Handling in Python?");
    addLog("");

    addLog("Answer:");
    addLog("File Handling is used to create, read, write, and update files.");

    addLog("");

    addLog("Example:");
    addLog("open('sample.txt', 'r')");
    addLog("file.read()");
    addLog("file.close()");

    addLog("");

    addLog("Program completed.");

}