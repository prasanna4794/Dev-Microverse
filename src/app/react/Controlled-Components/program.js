export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Controlled Components?");
    addLog("");

    addLog("Answer:");
    addLog("A Controlled Component is an input element whose value is controlled by React State.");

    addLog("");

    addLog("Example:");

    addLog("const [name, setName] = useState('');");

    addLog("");

    addLog("Program completed.");

}