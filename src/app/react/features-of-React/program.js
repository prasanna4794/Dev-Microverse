export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are the features of React?");
    addLog("");

    addLog("Answer:");

    addLog("1. Component-Based Architecture");
    addLog("2. JSX");
    addLog("3. Virtual DOM");
    addLog("4. Reusable Components");
    addLog("5. One-Way Data Flow");
    addLog("6. Fast Performance");

    addLog("");

    addLog("Program completed.");

}