export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is useEffect()?");
    addLog("");

    addLog("Answer:");
    addLog("useEffect() is a React Hook used to perform side effects such as API calls, timers, and event listeners.");

    addLog("");

    addLog("Example:");

    addLog(`useEffect(() => {

    console.log("Component Loaded");

}, []);`);

    addLog("");

    addLog("Program completed.");

}