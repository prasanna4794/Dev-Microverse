export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is useMemo()?");
    addLog("");

    addLog("Answer:");
    addLog("useMemo() is a React Hook that caches the result of expensive calculations to improve performance.");

    addLog("");

    addLog("Example:");
    addLog("const square = useMemo(() => count * count, [count]);");

    addLog("");

    addLog("Program completed.");

}