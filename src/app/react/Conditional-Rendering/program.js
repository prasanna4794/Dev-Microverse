export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Conditional Rendering in React?");
    addLog("");

    addLog("Answer:");
    addLog("Conditional Rendering is used to display different UI based on a condition.");

    addLog("");

    addLog("Example:");
    addLog("isLoggedIn = true");
    addLog("Output: Welcome");

    addLog("");

    addLog("Program completed.");

}