export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Props Drilling?");
    addLog("");

    addLog("Answer:");
    addLog("Props Drilling is the process of passing data from a parent component to deeply nested child components through intermediate components.");

    addLog("");

    addLog("Solution:");
    addLog("Use Context API to avoid Props Drilling.");

    addLog("");

    addLog("Program completed.");

}