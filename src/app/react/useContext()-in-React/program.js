export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is useContext()?");
    addLog("");

    addLog("Answer:");
    addLog("useContext() is a React Hook used to share data between components without passing props manually.");

    addLog("");

    addLog("Example:");
    addLog("const user = useContext(UserContext);");

    addLog("");

    addLog("Program completed.");

}