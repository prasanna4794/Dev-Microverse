export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Uncontrolled Components?");
    addLog("");

    addLog("Answer:");
    addLog("An Uncontrolled Component stores its value in the DOM instead of React State.");

    addLog("");

    addLog("Example:");

    addLog("const inputRef = useRef();");

    addLog("");

    addLog("Program completed.");

}