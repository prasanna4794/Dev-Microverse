export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is State in React?");
    addLog("");

    addLog("Answer:");
    addLog("State is a component's own data.");
    addLog("When the state changes, React automatically updates the UI.");
    addLog("");

    addLog("Example:");
    addLog("Count = 0");
    addLog("Click Button");
    addLog("Count = 1");

    addLog("");

    addLog("Program completed.");

}