export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is React?");
    addLog("");

    addLog("Answer:");
    addLog("React is a JavaScript library used to build fast, reusable and interactive user interfaces.");
    addLog("");

    addLog("Key Points:");
    addLog("- React is a JavaScript Library.");
    addLog("- Developed by Meta (Facebook).");
    addLog("- Uses reusable components.");
    addLog("- Fast rendering using Virtual DOM.");
    addLog("");

    addLog("Example:");

    addLog(`function App() {

    return <h1>Hello React</h1>;

}`);

    addLog("");

    addLog("Output:");
    addLog("Hello React");

    addLog("");

    addLog("Program completed.");

}