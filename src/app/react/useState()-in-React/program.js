export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is useState()?");
    addLog("");

    addLog("Answer:");
    addLog("useState() is a React Hook used to create and update state in functional components.");

    addLog("");

    addLog("Example:");

    addLog(`const [count, setCount] = useState(0);`);

    addLog("");

    addLog("Output:");
    addLog("Initial Value = 0");

    addLog("");

    addLog("Program completed.");

}