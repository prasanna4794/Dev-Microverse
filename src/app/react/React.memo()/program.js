export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is React.memo()?");
    addLog("");

    addLog("Answer:");
    addLog("React.memo() is a React feature that prevents unnecessary re-rendering of functional components.");

    addLog("");

    addLog("Example:");

    addLog("const Child = React.memo(function Child() {});");

    addLog("");

    addLog("Program completed.");

}