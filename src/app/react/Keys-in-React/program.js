export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Keys in React?");
    addLog("");

    addLog("Answer:");
    addLog("Keys are unique identifiers used to identify each item in a list.");

    addLog("");

    addLog("Example:");

    addLog(`<h2 key={user.id}>{user.name}</h2>`);

    addLog("");

    addLog("Program completed.");

}