export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are React Hooks?");
    addLog("");

    addLog("Answer:");
    addLog("React Hooks are special functions that allow functional components to use React features like state and lifecycle.");

    addLog("");

    addLog("Examples:");
    addLog("- useState()");
    addLog("- useEffect()");
    addLog("- useRef()");
    addLog("- useContext()");

    addLog("");

    addLog("Program completed.");

}