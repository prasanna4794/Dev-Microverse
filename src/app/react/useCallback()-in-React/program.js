export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is useCallback()?");
    addLog("");

    addLog("Answer:");
    addLog("useCallback() is a React Hook that caches a function to avoid unnecessary recreation.");

    addLog("");

    addLog("Example:");
    addLog("const handleClick = useCallback(() => {}, []);");

    addLog("");

    addLog("Program completed.");

}