export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is JSX?");
    addLog("");

    addLog("Answer:");
    addLog("JSX stands for JavaScript XML.");
    addLog("It allows us to write HTML-like syntax inside JavaScript.");
    addLog("");

    addLog("Example:");

    addLog(`function App() {

    return <h1>Hello React</h1>;

}`);

    addLog("");

    addLog("Output:");
    addLog("Hello React");

    addLog("");

    addLog("Program completed.");

}