export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Component in React?");
    addLog("");

    addLog("Answer:");
    addLog("A Component is a reusable UI block.");
    addLog("It helps us divide the UI into smaller reusable parts.");
    addLog("");

    addLog("Example:");

    addLog(`function Navbar() {

    return <nav>My Website</nav>;

}`);

    addLog("");

    addLog("Output:");
    addLog("My Website");

    addLog("");

    addLog("Program completed.");

}