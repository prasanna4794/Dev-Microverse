export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is useRef()?");
    addLog("");

    addLog("Answer:");
    addLog("useRef() is a React Hook used to directly access DOM elements.");

    addLog("");

    addLog("Example:");

    addLog("const inputRef = useRef(null);");

    addLog("");

    addLog("Program completed.");

}