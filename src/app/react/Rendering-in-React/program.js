export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is List Rendering in React?");
    addLog("");

    addLog("Answer:");
    addLog("List Rendering is used to display array data using the map() method.");

    addLog("");

    addLog("Example:");
    addLog("Students:");
    addLog("- Prasanna");
    addLog("- Kumar");
    addLog("- Arun");

    addLog("");

    addLog("Program completed.");

}