export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Conditional Rendering?");
    addLog("");

    addLog("Answer:");
    addLog("Conditional Rendering is the process of displaying different UI based on a condition.");

    addLog("");

    addLog("Example:");
    addLog("isLoggedIn ? 'Welcome' : 'Please Login'");

    addLog("");

    addLog("Program completed.");

}