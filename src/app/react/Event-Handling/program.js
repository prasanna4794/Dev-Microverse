export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Event Handling in React?");
    addLog("");

    addLog("Answer:");
    addLog("Event Handling is used to respond to user actions like clicks, typing, and form submissions.");

    addLog("");

    addLog("Example:");

    addLog(`<button onClick={handleClick}>Click Me</button>`);

    addLog("");

    addLog("Output:");
    addLog("Button Clicked");

    addLog("");

    addLog("Program completed.");

}