export default function runProgram(addLog) {

    addLog("Question:");
    addLog("How do you make API Calls in React?");
    addLog("");

    addLog("Answer:");
    addLog("API calls are usually made using fetch() or Axios inside useEffect().");

    addLog("");

    addLog("Example:");

    addLog("fetch('https://jsonplaceholder.typicode.com/users')");

    addLog("");

    addLog("Program completed.");

}