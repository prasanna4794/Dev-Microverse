export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is React Fragment?");
    addLog("");

    addLog("Answer:");
    addLog("React Fragment is used to group multiple JSX elements without adding an extra HTML element to the DOM.");

    addLog("");

    addLog("Example:");
    addLog("<> ... </>");

    addLog("");

    addLog("Program completed.");

}