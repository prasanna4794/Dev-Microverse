export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is INSERT in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("INSERT is used to add new records into a table.");

    addLog("");

    addLog("Example:");
    addLog("INSERT INTO Student VALUES (101, 'Prasanna', 23, 'Chennai');");

    addLog("");

    addLog("Output:");
    addLog("1 Row Inserted Successfully");

    addLog("");

    addLog("Program completed.");

}