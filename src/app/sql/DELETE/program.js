export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is DELETE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("DELETE is used to remove records from a table.");

    addLog("");

    addLog("Example:");
    addLog("DELETE FROM Student WHERE StudentID = 102;");

    addLog("");

    addLog("Output:");
    addLog("1 Row Deleted Successfully");

    addLog("");

    addLog("Program completed.");

}