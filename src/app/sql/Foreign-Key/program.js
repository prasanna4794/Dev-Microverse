export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Foreign Key?");
    addLog("");

    addLog("Answer:");
    addLog("A Foreign Key is a column that references the Primary Key of another table.");

    addLog("");

    addLog("Example:");
    addLog("Student Table -> StudentID (Primary Key)");
    addLog("Marks Table -> StudentID (Foreign Key)");

    addLog("");

    addLog("Program completed.");

}