export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is ORDER BY in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("ORDER BY is used to sort records in ascending or descending order.");

    addLog("");

    addLog("Example:");
    addLog("SELECT * FROM Student ORDER BY Age DESC;");

    addLog("");

    addLog("Output:");
    addLog("Arun      24");
    addLog("Prasanna  23");
    addLog("Rahul     22");

    addLog("");

    addLog("Program completed.");

}