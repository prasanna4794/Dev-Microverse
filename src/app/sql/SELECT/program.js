export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is SELECT in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("SELECT is used to retrieve data from a database table.");

    addLog("");

    addLog("Example:");
    addLog("SELECT * FROM Student;");

    addLog("");

    addLog("Output:");
    addLog("101  Prasanna  23  Chennai");
    addLog("102  Rahul     22  Madurai");

    addLog("");

    addLog("Program completed.");

}