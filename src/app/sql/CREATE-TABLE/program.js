export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is CREATE TABLE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("CREATE TABLE is used to create a new table in a database.");

    addLog("");

    addLog("Example:");
    addLog("CREATE TABLE Student (...)");

    addLog("");

    addLog("Columns:");
    addLog("StudentID");
    addLog("Name");
    addLog("Age");
    addLog("City");

    addLog("");

    addLog("Program completed.");

}