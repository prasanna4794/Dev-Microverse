export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is ALTER TABLE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("ALTER TABLE is used to modify the structure of an existing table.");

    addLog("");

    addLog("Example:");
    addLog("ALTER TABLE Student ADD City VARCHAR(50);");

    addLog("");

    addLog("Output:");
    addLog("Column Added Successfully");

    addLog("");

    addLog("Program completed.");

}