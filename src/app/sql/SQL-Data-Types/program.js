export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are SQL Data Types?");
    addLog("");

    addLog("Answer:");
    addLog("SQL Data Types define what kind of data can be stored in a table column.");

    addLog("");

    addLog("Example:");
    addLog("EmpID     -> INT");
    addLog("Name      -> VARCHAR");
    addLog("Salary    -> DECIMAL");
    addLog("JoinDate  -> DATE");

    addLog("");

    addLog("Program completed.");

}