export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is DROP TABLE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("DROP TABLE is used to permanently delete a table from the database.");

    addLog("");

    addLog("Example:");
    addLog("DROP TABLE Student;");

    addLog("");

    addLog("Output:");
    addLog("Table Dropped Successfully");

    addLog("");

    addLog("Program completed.");

}