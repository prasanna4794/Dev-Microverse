export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is SQL?");
    addLog("");

    addLog("Answer:");
    addLog("SQL (Structured Query Language) is used to communicate with databases. It allows us to store, retrieve, update, and delete data.");

    addLog("");

    addLog("Example:");
    addLog("SELECT * FROM Student;");

    addLog("");

    addLog("Output:");
    addLog("ID   Name       Age");
    addLog("1    Prasanna   23");
    addLog("2    Rahul      22");

    addLog("");

    addLog("Program completed.");

}