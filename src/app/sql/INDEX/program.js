export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is INDEX in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("INDEX is used to improve the speed of data retrieval.");

    addLog("");

    addLog("Example:");
    addLog("CREATE INDEX idx_employee ON Employee(Name);");

    addLog("");

    addLog("Output:");
    addLog("Index Created Successfully");

    addLog("");

    addLog("Program completed.");

}