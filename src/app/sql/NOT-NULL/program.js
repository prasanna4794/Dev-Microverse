export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is NOT NULL in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("NOT NULL prevents NULL (empty) values from being stored in a column.");

    addLog("");

    addLog("Example:");
    addLog("Name VARCHAR(50) NOT NULL");

    addLog("");

    addLog("Output:");
    addLog("Column 'Name' cannot be NULL");

    addLog("");

    addLog("Program completed.");

}