export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What are Rows and Columns in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("Rows represent records, and columns represent fields in a table.");

    addLog("");

    addLog("Example:");
    addLog("Student Table");

    addLog("");

    addLog("Output:");
    addLog("ID   Name       Age");
    addLog("1    Prasanna   23");
    addLog("2    Rahul      22");

    addLog("");

    addLog("Rows    : 2");
    addLog("Columns : 3");

    addLog("");

    addLog("Program completed.");

}