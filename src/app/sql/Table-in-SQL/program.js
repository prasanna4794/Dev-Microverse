export default function runProgram(addLog) {

    addLog("Question:");
    addLog("Why do we use SQL?");
    addLog("");

    addLog("Answer:");
    addLog("SQL is used to store, retrieve, update, and delete data from databases.");

    addLog("");

    addLog("Example:");
    addLog("SELECT * FROM Employee;");

    addLog("");

    addLog("Output:");
    addLog("ID   Name       Salary");
    addLog("1    Prasanna   50000");

    addLog("");

    addLog("Program completed.");

}