export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is TRUNCATE TABLE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("TRUNCATE TABLE removes all records from a table but keeps the table structure.");

    addLog("");

    addLog("Example:");
    addLog("TRUNCATE TABLE Student;");

    addLog("");

    addLog("Output:");
    addLog("Table Truncated Successfully");

    addLog("");

    addLog("Program completed.");

}