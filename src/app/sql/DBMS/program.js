export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is DBMS?");
    addLog("");

    addLog("Answer:");
    addLog("DBMS (Database Management System) is software used to create, manage, and maintain databases.");

    addLog("");

    addLog("Example:");
    addLog("DBMS : MySQL");
    addLog("Database : SchoolDB");
    addLog("Table : Student");

    addLog("");

    addLog("Program completed.");

}