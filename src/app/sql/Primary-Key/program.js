export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Primary Key?");
    addLog("");

    addLog("Answer:");
    addLog("A Primary Key uniquely identifies each record in a table.");

    addLog("");

    addLog("Example:");
    addLog("StudentID = 101");

    addLog("");

    addLog("Rules:");
    addLog("1. No Duplicate Values");
    addLog("2. No NULL Values");

    addLog("");

    addLog("Program completed.");

}