export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is RDBMS?");
    addLog("");

    addLog("Answer:");
    addLog("RDBMS (Relational Database Management System) stores data in related tables using relationships.");

    addLog("");

    addLog("Example:");
    addLog("Student Table -> Marks Table");
    addLog("Connected using StudentID");

    addLog("");

    addLog("Program completed.");

}