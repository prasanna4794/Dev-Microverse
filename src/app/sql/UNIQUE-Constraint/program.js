export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is UNIQUE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("UNIQUE prevents duplicate values in a column.");

    addLog("");

    addLog("Example:");
    addLog("Email VARCHAR(100) UNIQUE");

    addLog("");

    addLog("Output:");
    addLog("Duplicate Email Not Allowed");

    addLog("");

    addLog("Program completed.");

}