export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is UPDATE in SQL?");
    addLog("");

    addLog("Answer:");
    addLog("UPDATE is used to modify existing records in a table.");

    addLog("");

    addLog("Example:");
    addLog("UPDATE Student SET City='Coimbatore' WHERE StudentID=102;");

    addLog("");

    addLog("Output:");
    addLog("1 Row Updated Successfully");

    addLog("");

    addLog("Program completed.");

}