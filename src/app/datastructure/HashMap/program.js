export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a HashMap?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A HashMap is a data structure used to store data in key and value pairs."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "HashMap na data-va key and value pair-ah store pannum."
    );

    addLog("");

    addLog("Basic Structure:");

    addLog("Key → Value");

    addLog("");

    addLog("Example:");

    addLog("Name → Prasanna");
    addLog("Age → 25");
    addLog("City → Chennai");

    addLog("");

    addLog("Here:");
    addLog("Name, Age and City are Keys.");
    addLog("Prasanna, 25 and Chennai are Values.");

    addLog("");

    addLog("Example:");

    addLog('map.put("Name", "Prasanna");');
    addLog('map.put("Age", 25);');

    addLog("");

    addLog("Get Value:");

    addLog('map.get("Name") → Prasanna');
    addLog('map.get("Age") → 25');

    addLog("");

    addLog("Important Point:");
    addLog(
        "Each key should be unique in a HashMap."
    );

    addLog("");

    addLog("Real World Example:");
    addLog(
        "A phone contacts application can use a HashMap where the contact name is the key and the phone number is the value."
    );

    addLog("");

    addLog("Other Examples:");
    addLog("User ID → User Details");
    addLog("Product ID → Product Details");
    addLog("Employee ID → Employee Details");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A HashMap is a data structure that stores data in key-value pairs and provides fast lookup using the key."
    );

    addLog("");

    addLog("Program completed.");

}