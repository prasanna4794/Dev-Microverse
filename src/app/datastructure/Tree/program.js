export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Tree?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Tree is a data structure used to store data in a hierarchical structure."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Tree na data-va parent and child relationship-la organize pannura data structure."
    );

    addLog("");

    addLog("Basic Structure:");

    addLog("        10");
    addLog("       /  \\");
    addLog("      5    20");
    addLog("     / \\");
    addLog("    2   7");

    addLog("");

    addLog("Here:");
    addLog("10 is the Root.");
    addLog("5 and 20 are Children of 10.");
    addLog("2 and 7 are Children of 5.");

    addLog("");

    addLog("Important Terms:");
    addLog("Root → Top node of the tree.");
    addLog("Parent → A node that has children.");
    addLog("Child → A node connected to a parent.");
    addLog("Leaf → A node that has no children.");

    addLog("");

    addLog("Example:");

    addLog("Company");
    addLog("  → Engineering");
    addLog("      → Frontend");
    addLog("      → Backend");
    addLog("  → HR");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "A file system is a real-world example of a Tree where folders contain files and other folders."
    );

    addLog("");

    addLog("Other Examples:");
    addLog("File System");
    addLog("Company Hierarchy");
    addLog("HTML DOM");
    addLog("Organization Structure");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Tree is a non-linear data structure that organizes data in a hierarchical parent-child relationship."
    );

    addLog("");

    addLog("Program completed.");

}