export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Linked List?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Linked List is a data structure that stores data in connected nodes."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Linked List na data-va nodes-la store panni, each node-um next node-oda connect pannuvom."
    );

    addLog("");

    addLog("Basic Structure:");

    addLog("Node → Node → Node → null");

    addLog("");

    addLog("Each Node Contains:");
    addLog("1. Data");
    addLog("2. Reference to the next node");

    addLog("");

    addLog("Example:");

    addLog("10 → 20 → 30 → 40 → null");

    addLog("");

    addLog("Here:");
    addLog("10 is the first node.");
    addLog("20 is connected to 10.");
    addLog("30 is connected to 20.");
    addLog("40 is connected to 30.");
    addLog("null means there is no next node.");

    addLog("");

    addLog("Types of Linked List:");
    addLog("1. Singly Linked List");
    addLog("2. Doubly Linked List");
    addLog("3. Circular Linked List");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "A music playlist can be represented using a Linked List where each song points to the next song."
    );

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Linked List is a linear data structure where each node contains data and a reference to the next node."
    );

    addLog("");

    addLog("Program completed.");

}