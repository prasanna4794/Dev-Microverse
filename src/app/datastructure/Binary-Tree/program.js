export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Binary Tree?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Binary Tree is a tree data structure where each node can have at most two children."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Binary Tree na each node-ku maximum two children irukkum."
    );

    addLog("");

    addLog("Two Children:");
    addLog("1. Left Child");
    addLog("2. Right Child");

    addLog("");

    addLog("Basic Structure:");

    addLog("        10");
    addLog("       /  \\");
    addLog("      5    20");
    addLog("     / \\");
    addLog("    2   7");

    addLog("");

    addLog("Here:");
    addLog("10 is the Root.");
    addLog("5 is the Left Child of 10.");
    addLog("20 is the Right Child of 10.");
    addLog("2 and 7 are Children of 5.");

    addLog("");

    addLog("Important Point:");
    addLog(
        "A node can have zero, one, or two children."
    );

    addLog("");

    addLog("Example:");

    addLog("        A");
    addLog("       / \\");
    addLog("      B   C");
    addLog("     / \\");
    addLog("    D   E");

    addLog("");

    addLog("Here:");
    addLog("A has two children.");
    addLog("B has two children.");
    addLog("C has no children.");
    addLog("D and E are leaf nodes.");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "An expression tree can be represented using a Binary Tree where operators and values are stored as nodes."
    );

    addLog("");

    addLog("Important Uses:");
    addLog("Searching");
    addLog("Sorting");
    addLog("Expression Evaluation");
    addLog("Hierarchical Data");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Binary Tree is a tree data structure in which each node can have at most two children, called the left child and right child."
    );

    addLog("");

    addLog("Program completed.");

}