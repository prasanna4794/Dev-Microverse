export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Stack?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Stack is a data structure that follows the Last In First Out (LIFO) principle."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Stack na last-ah add panna data first-ah remove aagum."
    );

    addLog("");

    addLog("LIFO:");
    addLog("LIFO means Last In First Out.");

    addLog("");

    addLog("Example:");

    addLog("10");
    addLog("20");
    addLog("30");

    addLog("");

    addLog("If 30 is added last:");
    addLog("30 will be removed first.");

    addLog("");

    addLog("Stack Operations:");
    addLog("1. Push → Add an element");
    addLog("2. Pop → Remove the top element");
    addLog("3. Peek → View the top element");

    addLog("");

    addLog("Example:");

    addLog("Push(10)");
    addLog("Push(20)");
    addLog("Push(30)");

    addLog("");

    addLog("Stack:");
    addLog("30 ← Top");
    addLog("20");
    addLog("10");

    addLog("");

    addLog("Pop() → 30");
    addLog("Pop() → 20");
    addLog("Pop() → 10");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "A stack of plates is a simple real-world example. The last plate placed on top is the first plate removed."
    );

    addLog("");

    addLog("Other Examples:");
    addLog("Browser Back button");
    addLog("Undo operation");
    addLog("Function call stack");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Stack is a linear data structure that follows LIFO, which means the last element inserted is the first element removed."
    );

    addLog("");

    addLog("Program completed.");

}