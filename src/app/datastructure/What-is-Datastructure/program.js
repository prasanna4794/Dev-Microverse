export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Data Structure?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Data Structure is a way of organizing and storing data so that it can be accessed and modified efficiently."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Data Structure na data-va oru proper structure-la store and organize pannura method."
    );

    addLog("");

    addLog("Why do we use Data Structure?");
    addLog("1. Data-va organize panna.");
    addLog("2. Data-va fast-ah access panna.");
    addLog("3. Search operation efficient-ah panna.");
    addLog("4. Insert and delete operations manage panna.");
    addLog("5. Large amount of data-va efficiently handle panna.");

    addLog("");

    addLog("Examples:");
    addLog("Array");
    addLog("Linked List");
    addLog("Stack");
    addLog("Queue");
    addLog("HashMap");
    addLog("Tree");
    addLog("Graph");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "Phone contacts-la thousands of contacts irukkumbothu, avatrai proper-ah organize panni search panna Data Structures help pannum."
    );

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Data Structure is a way of organizing and storing data efficiently so that operations like access, search, insertion, deletion, and update can be performed effectively."
    );

    addLog("");

    addLog("Program completed.");

}