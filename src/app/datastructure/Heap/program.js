export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Heap?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Heap is a tree-based data structure used to quickly get the highest or lowest priority value."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Heap na priority based data-va store panna use pannura special tree structure."
    );

    addLog("");

    addLog("Types of Heap:");
    addLog("1. Min Heap");
    addLog("2. Max Heap");

    addLog("");

    addLog("Min Heap:");
    addLog(
        "In a Min Heap, the smallest value is always at the top."
    );

    addLog("");

    addLog("Example:");

    addLog("        10");
    addLog("       /  \\");
    addLog("      20   30");
    addLog("     / \\");
    addLog("    40  50");

    addLog("");

    addLog("Here:");
    addLog("10 is the smallest value.");
    addLog("10 is at the top.");

    addLog("");

    addLog("Max Heap:");
    addLog(
        "In a Max Heap, the largest value is always at the top."
    );

    addLog("");

    addLog("Example:");

    addLog("        50");
    addLog("       /  \\");
    addLog("      40   30");
    addLog("     / \\");
    addLog("    20  10");

    addLog("");

    addLog("Here:");
    addLog("50 is the largest value.");
    addLog("50 is at the top.");

    addLog("");

    addLog("Main Use:");
    addLog(
        "Heap is commonly used to implement a Priority Queue."
    );

    addLog("");

    addLog("Real World Example:");
    addLog(
        "A hospital emergency system can use a priority queue where patients with higher priority are handled first."
    );

    addLog("");

    addLog("Important Point:");
    addLog(
        "Heap is different from a Binary Search Tree. A Heap mainly focuses on priority, not complete sorted order."
    );

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Heap is a tree-based data structure used to maintain priority, where the minimum or maximum value can be accessed quickly."
    );

    addLog("");

    addLog("Program completed.");

}