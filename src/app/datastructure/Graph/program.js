export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Graph?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Graph is a data structure used to represent connections between different nodes."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Graph na nodes-um, andha nodes-ku between irukkura connections-um store panna use pannuvom."
    );

    addLog("");

    addLog("Main Parts:");
    addLog("1. Vertex (Node) → Represents an object.");
    addLog("2. Edge → Represents a connection between nodes.");

    addLog("");

    addLog("Example:");

    addLog("A ─── B");
    addLog("│     │");
    addLog("│     │");
    addLog("C ─── D");

    addLog("");

    addLog("Here:");
    addLog("A, B, C and D are Nodes.");
    addLog("The lines between them are Edges.");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "Google Maps can use a Graph where cities are nodes and roads between cities are edges."
    );

    addLog("");

    addLog("Other Examples:");
    addLog("Social Network");
    addLog("Google Maps");
    addLog("Computer Network");
    addLog("Flight Network");

    addLog("");

    addLog("Types of Graph:");
    addLog("1. Directed Graph");
    addLog("2. Undirected Graph");
    addLog("3. Weighted Graph");
    addLog("4. Unweighted Graph");

    addLog("");

    addLog("Important Point:");
    addLog(
        "A Graph is useful when we need to represent relationships or connections between data."
    );

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Graph is a non-linear data structure that consists of vertices and edges and is used to represent relationships between objects."
    );

    addLog("");

    addLog("Program completed.");

}