export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Sorting?");
    addLog("");

    addLog("Answer:");
    addLog(
        "Sorting is the process of arranging data in a specific order."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Sorting na data-va oru particular order-la arrange pannurathu."
    );

    addLog("");

    addLog("Types of Sorting Order:");

    addLog("1. Ascending Order");
    addLog("2. Descending Order");

    addLog("");

    addLog("Ascending Order:");

    addLog("Before:");
    addLog("[40, 10, 30, 20]");

    addLog("");

    addLog("After:");
    addLog("[10, 20, 30, 40]");

    addLog("");

    addLog("Descending Order:");

    addLog("Before:");
    addLog("[40, 10, 30, 20]");

    addLog("");

    addLog("After:");
    addLog("[40, 30, 20, 10]");

    addLog("");

    addLog("Common Sorting Algorithms:");

    addLog("1. Bubble Sort");
    addLog("2. Selection Sort");
    addLog("3. Insertion Sort");
    addLog("4. Merge Sort");
    addLog("5. Quick Sort");
    addLog("6. Heap Sort");

    addLog("");

    addLog("Why do we use Sorting?");
    addLog("1. Data-va organized-ah maintain panna.");
    addLog("2. Searching easy-ah panna.");
    addLog("3. Data compare panna.");
    addLog("4. Data processing improve panna.");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "Student marks can be sorted from highest to lowest to find the top students."
    );

    addLog("");

    addLog("Important Point:");
    addLog(
        "Different sorting algorithms have different time and space complexities."
    );

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "Sorting is the process of arranging data in a specific order, such as ascending or descending order."
    );

    addLog("");

    addLog("Program completed.");

}