export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Priority Queue?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Priority Queue is a data structure where elements are processed based on their priority."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Priority Queue na first-ah vandha element important illa, priority based-ah element process aagum."
    );

    addLog("");

    addLog("Normal Queue:");

    addLog("First In → First Out");

    addLog("");

    addLog("Priority Queue:");

    addLog("Highest Priority → First");

    addLog("");

    addLog("Example:");

    addLog("Patient A → Priority 2");
    addLog("Patient B → Priority 5");
    addLog("Patient C → Priority 1");

    addLog("");

    addLog("Processing Order:");

    addLog("Patient B → Priority 5");
    addLog("Patient A → Priority 2");
    addLog("Patient C → Priority 1");

    addLog("");

    addLog("How it works:");
    addLog("1. Add an element with a priority.");
    addLog("2. Compare the priorities.");
    addLog("3. Process the element with the highest priority.");
    addLog("4. Continue until all elements are processed.");

    addLog("");

    addLog("Common Implementation:");
    addLog(
        "Priority Queue is commonly implemented using a Heap."
    );

    addLog("");

    addLog("Types:");
    addLog("1. Min Priority Queue");
    addLog("2. Max Priority Queue");

    addLog("");

    addLog("Min Priority Queue:");
    addLog(
        "The element with the smallest priority value is processed first."
    );

    addLog("");

    addLog("Max Priority Queue:");
    addLog(
        "The element with the largest priority value is processed first."
    );

    addLog("");

    addLog("Real World Example:");
    addLog(
        "A hospital emergency system can use a Priority Queue to treat patients with higher priority first."
    );

    addLog("");

    addLog("Other Examples:");
    addLog("CPU Scheduling");
    addLog("Printer Scheduling");
    addLog("Network Traffic");
    addLog("Emergency Systems");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Priority Queue is a data structure where elements are processed according to their priority instead of their insertion order."
    );

    addLog("");

    addLog("Program completed.");

}