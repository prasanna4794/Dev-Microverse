export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is an Array?");
    addLog("");

    addLog("Answer:");
    addLog(
        "An Array is a data structure used to store multiple values in a single variable."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Array na multiple values-ah oru single variable-la store panna use pannuvom."
    );

    addLog("");

    addLog("Example:");

    addLog("const numbers = [10, 20, 30, 40, 50];");

    addLog("");

    addLog("Array Elements:");
    addLog("Index 0 → 10");
    addLog("Index 1 → 20");
    addLog("Index 2 → 30");
    addLog("Index 3 → 40");
    addLog("Index 4 → 50");

    addLog("");

    addLog("Important Point:");
    addLog(
        "Array index usually starts from 0."
    );

    addLog("");

    addLog("Example Access:");

    addLog("numbers[0] → 10");
    addLog("numbers[2] → 30");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "Student marks, employee names, product prices, and user IDs can be stored in an Array."
    );

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "An Array is a data structure that stores multiple elements and allows us to access elements using an index."
    );

    addLog("");

    addLog("Program completed.");

}