export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is Space Complexity?");
    addLog("");

    addLog("Answer:");
    addLog(
        "Space Complexity is used to measure how much memory an algorithm needs as the input size grows."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Space Complexity na program run aagumbothu evlo extra memory use aaguthu-nu measure pannurathu."
    );

    addLog("");

    addLog("Why do we use Space Complexity?");
    addLog("1. Memory usage understand panna.");
    addLog("2. Memory efficient algorithm choose panna.");
    addLog("3. Large data handle panna.");
    addLog("4. Extra memory usage reduce panna.");

    addLog("");

    addLog("Common Space Complexities:");

    addLog("O(1) → Constant Space");
    addLog("O(n) → Linear Space");
    addLog("O(n²) → Quadratic Space");

    addLog("");

    addLog("Example - O(1):");

    addLog("int sum = 0;");
    addLog("int count = 0;");

    addLog(
        "Only a fixed amount of extra memory is used."
    );

    addLog("");

    addLog("Example - O(n):");

    addLog("int[] numbers = new int[n];");

    addLog(
        "Memory increases when the input size increases."
    );

    addLog("");

    addLog("Example:");

    addLog("Input size = 5");
    addLog("Extra memory = 5 elements");

    addLog("");

    addLog("Input size = 100");
    addLog("Extra memory = 100 elements");

    addLog("");

    addLog("Important Point:");
    addLog(
        "Lower space complexity usually means the algorithm uses less extra memory."
    );

    addLog("");

    addLog("Time vs Space Complexity:");
    addLog("Time Complexity → Measures execution time.");
    addLog("Space Complexity → Measures memory usage.");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "Space Complexity is a measure of how much extra memory an algorithm requires as the input size increases."
    );

    addLog("");

    addLog("Program completed.");

}