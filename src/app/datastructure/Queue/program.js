export default function runProgram(addLog) {

    addLog("Question:");
    addLog("What is a Queue?");
    addLog("");

    addLog("Answer:");
    addLog(
        "A Queue is a data structure that follows the First In First Out (FIFO) principle."
    );

    addLog("");

    addLog("Simple Explanation:");
    addLog(
        "Queue na first-ah add panna data first-ah remove aagum."
    );

    addLog("");

    addLog("FIFO:");
    addLog("FIFO means First In First Out.");

    addLog("");

    addLog("Example:");

    addLog("10");
    addLog("20");
    addLog("30");

    addLog("");

    addLog("If 10 is added first:");
    addLog("10 will be removed first.");

    addLog("");

    addLog("Queue Operations:");
    addLog("1. Enqueue → Add an element");
    addLog("2. Dequeue → Remove an element");
    addLog("3. Front → View the first element");

    addLog("");

    addLog("Example:");

    addLog("Enqueue(10)");
    addLog("Enqueue(20)");
    addLog("Enqueue(30)");

    addLog("");

    addLog("Queue:");
    addLog("Front → 10 → 20 → 30 ← Rear");

    addLog("");

    addLog("Dequeue() → 10");
    addLog("Dequeue() → 20");
    addLog("Dequeue() → 30");

    addLog("");

    addLog("Real World Example:");
    addLog(
        "People waiting in a ticket line are a simple example of a Queue. The person who comes first is served first."
    );

    addLog("");

    addLog("Other Examples:");
    addLog("Printer Queue");
    addLog("Ticket Booking");
    addLog("CPU Scheduling");
    addLog("Message Processing");

    addLog("");

    addLog("Interview Answer:");
    addLog(
        "A Queue is a linear data structure that follows FIFO, which means the first element inserted is the first element removed."
    );

    addLog("");

    addLog("Program completed.");

}